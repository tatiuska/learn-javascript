// função que retorna o número de ítens contidos numa array
function getNumberOfElements(elements) {
    return elements.length;
};

console.log(getNumberOfElements(['a', 'b', 'c']));
console.log(getNumberOfElements([]));